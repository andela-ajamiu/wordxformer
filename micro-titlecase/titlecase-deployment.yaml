apiVersion: extensions/v1beta1

kind: Deployment

metadata:
  name: titlecase-dep

spec:
  replicas: 3

  template:

    metadata:
      labels:
        name: titlecase-pods

    spec:
      containers:
      - image: micro-titlecase:1.0
        name: titlecase-container